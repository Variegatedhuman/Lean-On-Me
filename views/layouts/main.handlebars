<html lang="en">

  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Lean On Me</title>
      </head>
  <body>

    <div class="wrapper">
      <header>
        <h1>The Tech Side of Things</h1>
        <nav class="navbar">
          {{loggedIn}}
          {{#if loggedIn}}
            <a href="/" class="homepage">Homepage</a>
            <a href="/dashboard">Dashboard</a>
            <a href="/logout" class="nav-link">Logout</a>
          {{else}}
            <a href="/" class="homepage">Homepage</a>
            <a href="/login">Login</a>
          {{/if}}
        </nav>
      </header>
      <div class="row">

        <div class="col-md-12">
          <h1>{{title}}</h1>
        </div>
      </div>
      <div class="row">
        <div class="col-md-12">
          {{{body}}}
        </div>
      </div>
    
    </div>
   
   
  </div>

  {{#if loggedIn}}
  <script src="/public/javascript/logout.js"></script>
  {{/if}}
 
</body>

</html>